import { IAppSliceInitialState } from "../interfaces/app-slice-interface";
import { APP_LANGUAGE } from "./app-constants";

export const APP_SLICE_INITIAL_STATE: IAppSliceInitialState = {
  appLocale: APP_LANGUAGE.EN
};
